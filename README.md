# SwiftDecision

SwiftDecision is being developed as a typed, model-assisted decision library built on SpecificationCore.

The package builds against SpecificationCore `1.1.0` and verifies its asynchronous decision and first-match APIs. Model backends and decision policies are the next layer to add.

## Build

```sh
swift build
swift test
```
